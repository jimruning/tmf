# 🔑 TMF系统登录指南

**快速登录 - 2分钟上手**

---

## 🚀 方法1：使用默认账户（最简单）

### 步骤：

#### 1️⃣ 启动系统
```bash
# 双击运行启动脚本
start.bat
```
等待浏览器自动打开

#### 2️⃣ 访问登录页面
浏览器会自动打开：
```
http://localhost:3000
```

或者手动访问这个地址

#### 3️⃣ 输入登录信息
在登录表单中填写：
```
用户名：admin
密码：admin123
```

#### 4️⃣ 点击"登录"按钮
- 等待1-2秒
- 看到"登录成功"提示
- 自动跳转到主页面

#### ✅ 登录成功！
你会看到：
- 顶部显示："欢迎, admin"
- 左侧：TMF文件夹树（11个区域）
- 右侧：文件列表区域
- 右上角：退出按钮

---

## 📝 默认账户信息

```
用户名：admin
密码：admin123
角色：管理员（admin）
```

⚠️ **重要提示：**
- 这是默认的测试账户
- 仅用于开发和测试
- 生产环境必须修改密码

---

## 🖼️ 登录界面说明

### 登录页面布局
```
┌─────────────────────────────────┐
│                                 │
│      TMF文档管理系统             │
│    临床试验主文档管理            │
│                                 │
├─────────────────────────────────┤
│                                 │
│  👤 [用户名输入框]               │
│                                 │
│  🔒 [密码输入框]                 │
│                                 │
│  [   登 录   ]  按钮            │
│                                 │
│  MVP版本 - 简化登录（仅演示）     │
│                                 │
└─────────────────────────────────┘
```

### 输入说明
- **用户名框：** 输入 `admin`
- **密码框：** 输入 `admin123`（字符会显示为圆点•••）
- **登录按钮：** 点击后等待响应

---

## 🔧 登录前检查清单

### ✅ 确保系统已启动

#### 检查后端（必须）
打开新的命令提示符窗口：
```bash
curl http://localhost:3001/health
```
或直接在浏览器访问：
```
http://localhost:3001/health
```

**应该看到：**
```json
{
  "status": "ok",
  "message": "TMF Backend API is running",
  "timestamp": "2025-01-15T..."
}
```

#### 检查前端（必须）
浏览器访问：
```
http://localhost:3000
```

**应该看到：** 登录页面

---

## ❌ 登录失败排查

### 问题1：页面无法打开
**症状：** 浏览器显示"无法访问此网站"

**解决：**
```bash
# 1. 确认前端已启动
# 应该看到一个终端窗口显示：
# "Compiled successfully!"
# "Local: http://localhost:3000"

# 2. 如果没有启动，手动运行：
cd frontend
npm start

# 3. 等待编译完成（约30秒）
# 4. 浏览器会自动打开
```

---

### 问题2：登录没反应
**症状：** 点击登录按钮后没有任何提示

**解决：**
```bash
# 1. 检查后端是否运行
# 访问：http://localhost:3001/health

# 2. 如果后端未启动，运行：
cd backend
npm start

# 3. 等待看到：
# "🚀 TMF 后端服务器启动成功"
# "📍 地址: http://localhost:3001"

# 4. 刷新浏览器页面
# 5. 重新尝试登录
```

---

### 问题3：用户名或密码错误
**症状：** 显示"用户名或密码错误"

**可能原因：**
1. 输入了错误的用户名或密码
2. 数据库未初始化
3. 已修改过密码但忘记了新密码

**解决：**
```bash
# 方案1：重新输入（注意大小写）
用户名：admin  （全部小写）
密码：admin123

# 方案2：重新初始化数据库
cd backend
del tmf.db
npm run init-db

# 看到输出：
# ✅ 默认管理员账户创建成功 (用户名: admin, 密码: admin123)

# 方案3：修改密码
双击运行：change-password.bat
按照提示重置密码
```

---

### 问题4：CORS错误
**症状：** 浏览器控制台显示CORS policy错误

**解决：**
```bash
# 1. 确认后端正在运行
# 2. 确认后端端口是3001
# 3. 确认前端端口是3000
# 4. 重启后端：
cd backend
npm start

# 5. 清除浏览器缓存并刷新
# 按 Ctrl+Shift+Delete
# 清除缓存
# 刷新页面
```

---

## 🔄 登录后做什么

### 登录成功后，你可以：

#### 1️⃣ 浏览TMF文件夹结构
```
左侧显示：
01 Trial Management
02 Central Trial Documents
03 Regulatory
... 共11个区域
```
- 点击展开/折叠
- 查看中英文双语名称
- 选择具体文档位置

#### 2️⃣ 上传文件
- 选择一个文件夹
- 点击"上传文件"按钮
- 选择文件上传

#### 3️⃣ 查看和管理文件
- 查看文件列表
- 下载文件
- 删除文件（软删除）

#### 4️⃣ 修改密码
- 建议首次登录后立即修改
- 运行 `change-password.bat`

---

## 🔒 安全最佳实践

### ⚠️ 开发环境
```
✅ 使用默认账户 admin/admin123
✅ 仅在本地测试
✅ 不要在公网暴露
```

### ⚠️ 生产环境
```
❌ 严禁使用默认密码
✅ 立即修改管理员密码
✅ 使用强密码（8位以上，包含大小写字母+数字）
✅ 启用HTTPS
✅ 定期更换密码
```

---

## 📱 不同环境登录

### 本地开发
```
前端：http://localhost:3000
后端：http://localhost:3001
账户：admin / admin123
```

### 局域网访问
```
前端：http://你的IP:3000
后端：http://你的IP:3001
账户：admin / admin123
```
（需要修改CORS配置）

### 生产部署
```
使用域名访问
自定义管理员账户
强密码策略
HTTPS加密
```

---

## 🎓 登录流程图

```
用户打开浏览器
       ↓
访问 http://localhost:3000
       ↓
看到登录页面
       ↓
输入用户名：admin
输入密码：admin123
       ↓
点击"登录"按钮
       ↓
前端发送请求到后端
POST http://localhost:3001/api/auth/login
       ↓
后端验证用户名和密码
       ↓
验证成功，生成JWT Token
       ↓
返回Token和用户信息
       ↓
前端保存Token到localStorage
       ↓
显示"登录成功"提示
       ↓
自动跳转到 /dashboard
       ↓
✅ 登录完成！
```

---

## 💡 快速参考

### 一键启动脚本
```bash
# 安装依赖
install.bat

# 初始化数据库
init-db.bat

# 启动系统
start.bat
```

### 登录URL
```
http://localhost:3000
```

### 默认账户
```
用户名：admin
密码：admin123
```

### API端点
```
登录：POST /api/auth/login
注册：POST /api/auth/register
```

---

## 🆘 需要帮助？

### 常见问题
- 查看浏览器控制台错误（F12）
- 查看后端终端日志
- 查看前端终端日志

### 重置系统
```bash
# 1. 停止所有服务（Ctrl+C）
# 2. 删除数据库
cd backend
del tmf.db
# 3. 重新初始化
npm run init-db
# 4. 重新启动
cd ..
start.bat
```

### 完整测试
```bash
# 按顺序执行
1. install.bat      (安装)
2. init-db.bat      (初始化)
3. verify-data.bat  (验证)
4. start.bat        (启动)
5. 浏览器登录       (测试)
```

---

## ✅ 登录检查清单

登录前：
- [ ] 后端已启动（http://localhost:3001/health 返回ok）
- [ ] 前端已启动（http://localhost:3000 显示登录页）
- [ ] 数据库已初始化（tmf.db文件存在）

登录时：
- [ ] 输入正确的用户名（admin）
- [ ] 输入正确的密码（admin123）
- [ ] 点击登录按钮
- [ ] 等待响应

登录后：
- [ ] 看到"登录成功"提示
- [ ] 自动跳转到dashboard
- [ ] 显示文件夹树
- [ ] 右上角显示用户名

---

**登录指南版本：** v1.0
**最后更新：** 2025-01-15

🎯 **准备好了吗？运行 `start.bat` 开始登录！**
